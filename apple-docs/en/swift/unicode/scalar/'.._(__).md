---
title: '..<(_:)'
framework: Swift
symbol_kind: op
role: symbol
role_heading: Operator
platforms: [iOS 8.0+, iPadOS 8.0+, Mac Catalyst 13.0+, macOS 10.10+, tvOS 9.0+, visionOS 1.0+, watchOS 2.0+]
languages: [swift, swift]
beta: false
deprecated: false
doc_path: '/documentation/swift/unicode/scalar/''.._(_:)'
source_url: 'https://developer.apple.com/documentation/swift/unicode/scalar/''.._(_:)'
doc_json: 'https://developer.apple.com/tutorials/data/documentation/swift/unicode/scalar/%27.._%28_%3A%29.json'
content_hash: 'sha256:aef148cf6153d074'
translated: false
---

> Navigation: [Technologies](../../../technologies.md) · [Swift](../../../swift.md) · [Unicode](../../unicode.md) · [Scalar](../scalar.md)

# ..\<(_:)

<sub>Operator</sub>

Returns a partial range up to, but not including, its upper bound.

<sub>iOS, iPadOS, Mac Catalyst, macOS, tvOS, visionOS, watchOS</sub>

```swift
static func ..< (maximum: Self) -> PartialRangeUpTo<Self>
```

## Parameters

- `maximum` — The upper bound for the range.

## Discussion

Use the prefix half-open range operator (prefix `..<`) to create a partial range of any type that conforms to the `Comparable` protocol. This example creates a `PartialRangeUpTo<Double>` instance that includes any value less than `5.0`.

```swift
let upToFive = ..<5.0

upToFive.contains(3.14)       // true
upToFive.contains(6.28)       // false
upToFive.contains(5.0)        // false
```

You can use this type of partial range of a collection’s indices to represent the range from the start of the collection up to, but not including, the partial range’s upper bound.

```swift
let numbers = [10, 20, 30, 40, 50, 60, 70]
print(numbers[..<3])
// Prints "[10, 20, 30]"
```

> [!info] Precondition
> `maximum` must compare equal to itself (i.e. cannot be NaN).

## See Also

### Creating Ranges of Scalars

- [...(_:)](<'...(__)-9u9rz.md>) — Returns a partial range extending upward from a lower bound.
- [...(_:)](<'...(__)-7lhvp.md>) — Returns a partial range up to, and including, its upper bound.
- [...(_:_:)](<'...(____).md>) — Returns a closed range that contains both of its bounds.
- [..\<(_:_:)](<'.._(____).md>) — Returns a half-open range that contains its lower bound but not its upper bound.
