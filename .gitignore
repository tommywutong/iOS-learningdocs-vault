.cache/
.DS_Store
__pycache__/
*.pyc

# 抓取暂存区：这是给旧仓库 apple-developer-archive-vault 准备的产物，
# 核对后会整体移过去，不属于本仓库
.staging/

# 翻译分片：shard.py 每轮按当前待译清单重新生成，
# 是派生状态而不是事实来源（事实来源是 zh/ 下到底有没有那个文件）
meta/shards/

# 压缩过程的备份文件（shrink_assets.py 正常结束会自己删，
# 中途被杀才会残留——它们是未压缩的原图，绝不能进仓库）
*.orig
