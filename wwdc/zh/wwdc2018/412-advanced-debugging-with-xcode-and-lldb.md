---
title: 使用 Xcode 与 LLDB 的高级调试
session_id: 412
collection: wwdc2018
year: 2018
duration: '53:41'
topics: [Developer Tools]
group: G · 调试、崩溃与 Instruments
evergreen: true
source_url: 'https://developer.apple.com/videos/play/wwdc2018/412/'
content_hash: 'sha256:923bfde46b5926a6'
translated: true
---

# 使用 Xcode 与 LLDB 的高级调试

<sub>WWDC2018 · 53:41 · 开发者工具</sub>

探索高级技术以及增强 Xcode 调试工作流程的提示与技巧。学习如何充分利用 LLDB 和……

> [!note] 归档理由
> LLDB 表达式求值、断点动作，调试能力天花板

## 相关资源

- [使用脚本向 LLDB 添加自定义命令](https://developer.apple.com/sample-code/wwdc/2018/UseScriptsToAddCustomCommandsToLLDB.zip)
- [HD 视频](https://devstreaming-cdn.apple.com/videos/wwdc/2018/412zw88j5aa4mr9/412/412_hd_advanced_debugging_with_xcode_and_lldb.mp4?dl=1)
- [SD 视频](https://devstreaming-cdn.apple.com/videos/wwdc/2018/412zw88j5aa4mr9/412/412_sd_advanced_debugging_with_xcode_and_lldb.mp4?dl=1)
- [演示幻灯片（PDF）](https://devstreaming-cdn.apple.com/videos/wwdc/2018/412zw88j5aa4mr9/412/412_advanced_debugging_with_xcode_and_lldb.pdf?dl=1)
- [使用 LLDB 调试 Swift 调试](https://developer.apple.com/videos/play/wwdc2022/110370)
- [了解断点改进](https://developer.apple.com/videos/play/wwdc2021/10209)
- [Xcode 11 中的调试](https://developer.apple.com/videos/play/wwdc2019/412)
- [Xcode 入门](https://developer.apple.com/videos/play/wwdc2019/404)
- [LLDB：超越“po”](https://developer.apple.com/videos/play/wwdc2019/429)
- [iOS 内存深度剖析](https://developer.apple.com/videos/play/wwdc2018/416)
- [理解崩溃与崩溃日志](https://developer.apple.com/videos/play/wwdc2018/414)

## 逐字稿

> [!warning] 关于逐字稿
> 这份逐字稿是 Apple 的自动语音识别产物，**未经人工校对**，可能有术语转写错误。段落已按原始 HTML 的 `<p>` 结构重组，但断句仍可能不自然。

你好，欢迎来到使用 Xcode 与 LLDB 的高级调试。

我是 Chris Miles，Xcode 团队的工程经理之一。今天能来到这里我非常兴奋。我很清楚，在 Beer Bash 和你之间，只隔着我们的这场讲座，所以谢谢你们能来。我们会准时结束，但今天为你们准备了很多精彩内容。让我们直接开始吧。我想先谈谈 Swift 调试的可靠性。这里要说的重点是，有好消息。团队在 Xcode 10 中落地了大量可靠性修复。

—— 谢谢。

编译器和调试器团队已经平滑处理了许多导致 Swift 调试困难的粗糙边缘情况。我想告诉你们其中的几个。在某些情况下，通常是对于更复杂的项目或构建配置，尝试在控制台中 po 一个对象或求值表达式可能会失败，并出现类似这样的错误。这个错误所指的 AST 上下文是一个表达式上下文，LLDB 需要用它来重建构建项目时编译器的状态。在某些情况下，例如存在模块冲突时，表达式上下文无法可靠地重建，你的表达式就会失败。

在 Xcode 10 中，LLDB 实现了一种当这种问题发生时使用的回退机制。所以，如果它无法重建上下文，它会回退到为当前帧创建一个更简单的上下文，并用它来求值你的表达式。

另一个一些开发者可能遇到过的失败情况是，调试器在调试时无法物化变量类型。

这在 Xcode 中会表现为类似这样。在左边，你可以看到变量视图（Variables view）显示了帧中所有变量的名称，但看不到任何类型信息或值。尝试打印变量值时会失败，并出现像这样的错误。现在，再次很大程度上要感谢你们的错误报告，团队已经能够追踪并修复这些调试信息无法可靠生成的许多边界情况。所以，我想代表团队再次感谢你们在调试时遇到任何问题都提交反馈。如果你们在调试项目时发现 Xcode 10 有任何其他问题，请继续为我们提交错误报告。对于在座的参会者，如果在你的项目中发现问题，请来实验室。我们有一个 Xcode 调试与性能分析实验室，明天上午九点到十二点。带上你的项目，找一位 Xcode 调试器工程师或 LLDB 工程师，他们非常乐意查看你的项目。

现在我想继续讲讲一些我最喜欢的调试技巧和窍门，我常用它们来增强我的调试工作流程。

实际上，我不只是要告诉你，我还想用一个演示来展示给你看。

我们今天要使用的项目是一个名为“Solar System”的 iOS App。你可能在 Keynote 和 State of the Union 这样的主题演讲中见过“Solar System”。我们将调试“Solar System”中一个叫做“Moon Jumper”的区域。

Moon Jumper 允许用户手持手机并在空中跳跃。

该 App 测量你跳跃的力度，然后将其转换为月球重力，并向你展示一个视觉效果，显示如果你站在月球上，你能跳多高。

你可以设置一个横条来选择高度限制，这样你就可以尝试挑战自己，以月球重力为单位，跳得和横条一样高。

在对 Moon Jumper 进行一些增强（例如一些视觉增强和一种 GamePlay 模式）时，虽然我们还没有准备好发布，但我们进行了一次测试遍历，并整理出了一份需要查看的错误列表。这些就是 Solar System 的错误。我将首先处理 iOS 错误，稍后 Sebastian 会出来解决 macOS 错误。现在，正如所说，在我们修复所有这些错误之前，我们谁都别想去 Beer Bash，所以没有比和 2000 人一起结对编程更令人兴奋的事了。

让我们直接开始，先处理第一个错误。跳跃失败动画与规格不符。

这说的是什么呢？切换到模拟器，因为我们正在使用模拟器来加速调试和开发。我已经设置了一个点击手势来识别它们，所以每次我点击宇航员，他都会成功跳到横条的高度。

现在这个错误说的是宇航员没有达到横条高度的情况，所以让我们重现它。

切换到编辑器，我将使用跳转栏导航到 jump 函数，并在该函数的开头设置一个断点。

现在我将点击宇航员，我们将开始一次跳跃，现在我们暂停在调试器中。

首先要指出的是这里，在标签页栏里。

我们现在有四个标签页。这个调试标签页是 Xcode 刚刚为我们创建的，对于那些像我一样喜欢使用标签页工作的人来说，这是一种你可以定义的 Xcode 行为。

要做到这一点，你需要使用 Xcode 菜单来编辑行为，这会带你进入偏好设置的“行为”标签页。在这里你可以配置很多行为。在这种情况下，你需要在“运行”部分配置暂停时的行为。这是当 Xcode 在调试器中暂停时将执行的行为。所以你可以看到，我在这里配置了显示一个名为“debug”的标签页，并且当在调试器中暂停时，Xcode 将始终切换到该标签页。这对于像我一样喜欢使用标签页的用户来说非常棒。

现在切换回代码，我们可以看到有一个基于这个属性 didReachSelectedHeight 的条件，所以我想看看这个属性的值。切换到调试控制台，我可以使用 po 来查看那个属性的值，它当前设置为 true。所以这里总是将其设置为 true，我想把它改成 false，这样我们就可以重现这个错误。我可以去代码里修改它，并点触手势将其设置为 false，但如果可以避免，我不喜欢仅仅为了调试而修改代码。所以在这种情况下，我可以使用调试器来帮我做这件事。我可以使用 expression 命令。在这里我可以给它任何 Swift 表达式，比如 didReachSelectedHeight = false，它会求值并执行它。所以现在，我们可以看到这个属性确实变成了 false。

如果我使用调试器单步执行这一行，我们可以看到我们已经进入了分支的 false 这一侧。

所以现在，当我们继续时，我们可以看到宇航员没有完全达到横条的高度并掉了下来。

这就是我们试图重现的情况。我希望每次我点击宇航员时都会发生这种情况，而且我不想每次都停下来手动输入这个表达式，所以我要配置这个断点来为我做这件事。

如果我右键点击断点，我可以选择“编辑断点”，这会给我一个弹出窗口，其中包含一些配置选项，用于自定义断点的行为。我将选择“调试器命令”动作，并输入我在调试控制台中使用的 expression 命令，然后将其设置为自动继续的断点。所以我们在这里配置的是一个断点，一旦执行进入这个函数，断点就会触发，为我们执行这个命令来修改属性的值，然后自动继续。

所以现在，每次我点击宇航员，他都会执行一次不成功的跳跃并摔回来。

现在我们需要修复的问题是什么？规格说明说，摔倒后，宇航员应该重新站起来，所以我们现在就来修复它。

我将导航到这个函数 updateUIForJumpFailed，我们可以看到这个函数使用 UIKit Dynamics 来模拟跳跃失败。它首先创建一个 UIDynamicAnimator，然后调用一个函数来添加行为以创建物理效果。然后宇航员应该在 dynamicAnimatorDidPause 委托回调中被重新定向和居中。我们可以向下滚动，看到这个委托回调已经成功实现，所以看起来没问题。

但我注意到这个对象上没有设置委托，所以如果我在代码里加上这一行，我认为这就是修复问题所需的修改。现在，我可以重新编译并重新运行，然后尝试验证我的修复，但如果可以的话，我想缩短整个周期。所以我要做的是使用一个断点来为我注入这个修改，这样我就可以快速方便地看看这是否能解决问题。

所以我会创建一个断点，双击以调出编辑窗口，这是调出编辑窗口的一个快捷方式。然后，再次使用一个调试器命令动作来注入一个 expression，我可以输入我认为能解决问题的这行代码，并将其设置为一个自动继续的断点。

我在这里配置的是，尽管我在代码中做了修改，但我当然还没有重新编译。所以我使用自定义断点来为我注入那个修改，这样我就可以使用当前正在运行的 App 来测试它。所以现在，如果我点击宇航员，他会执行一次不成功的跳跃，然后摔倒并再次站起来，看起来我们解决了问题。我喜欢这个效果，所以我要再来一次。

现在让我再次启动 Notes。

我们可以把第一个错误标记为已修复。

没有什么比勾掉已修复的错误更好的了。

接下来的三个错误与我在做的新的 GamePlay 模式有关。所以我要在模拟器中按播放来向你们展示，这里的挑战是尝试比横条跳得更高，共 10 次。而且横条一开始很低，每次都会升高。

你也可以看到顶部添加了一些分数标签。所以如果我点击宇航员，你会看到他仍然配置为执行不成功的跳跃，然后顶部的尝试次数标签应该增加，但它没有。这就是我们遇到的第一个错误，这些标签会闪烁但不会改变。我们还有一个关于游戏结束状态未正确处理的问题，以及那些分数和尝试标签的布局问题，但我们稍后会回到那些。

回到这个错误，如果你没注意到，让我再点一下宇航员，注意看顶部尝试标签。

你会看到它确实闪烁了，但没有更新。这告诉我，标签被设置了一个值，因为我们看到了过渡动画，但值不正确。所以我想找到修改这个标签的代码，看看那点的逻辑是什么。

我们注意到 UILabel 的 text 属性被改变了，所以我要做的是切换到断点导航器（Breakpoint navigator），然后在底部这里选择这个加号按钮来创建许多专门的断点之一。你可以看到我们有针对 Swift 错误和异常甚至测试值的断点，但针对这种情况，我将使用符号断点（Symbolic Breakpoint）。

这会创建一个新的断点并调出其编辑器，在这里我们可以输入任何函数或方法名称，比如我们需要用到的 `-[UILabel setText:]`，并以 Objective-C 格式输入它，因为 UIKit 是 Objective-C 框架。

需要指出的是，如果我关闭它，在断点下方会有一个已添加的条目，这是调试器给我们的反馈，告诉我们它能够将这个断点解析到 UIKit Core 中的一个位置。

有些符号可能会解析出多个位置，你会在这里看到全部。如果你看到没有条目，那就表示调试器无法解析你的断点，所以它不会命中。

配置好之后，我将再次点击宇航员，现在我们看到我们命中了 `-[UILabel setText:]` 上的断点。当然，我们没有 UIKit 的源代码，所以我们看到的是汇编代码，但即使是处于某个系统框架的汇编代码中，也无需保持盲目。我们可以检查传递给函数的参数。你只需要知道该架构的调用约定，然后检查寄存器来查看参数。好吧，我承认我从来不记得那些寄存器是什么，但谢天谢地，我不需要记住，因为调试器提供了伪寄存器（pseudo-registers）。

`$arg1` 被翻译成保存第一个参数的寄存器。所以在这种情况下，我们可以看一下那个 Objective-C 消息的接收者，它是一个 UILabel 实例。

现在我们看到它的值是“17 feet”，这告诉我它是这里的高度标签，所以不是我们感兴趣的标签，但既然我们在这里，让我们看看其他参数。

如果你熟悉 Objective-C 消息发送，你可能会记得第二个参数应该是选择器。

我们在这里看不到，那是因为 LLDB 没有隐式地知道这些参数的类型。所以在某些情况下，我们需要对它进行类型转换，现在我们看到了这个消息的选择器。第三个参数是传入该方法的第一个参数。换句话说，它是传入 `setText` 的字符串。所以如果你想检查某个框架的汇编帧中的参数，这是一种极大的便利。

但就像我说的，这不是我们感兴趣的对象或标签，所以我们按继续，现在我们再次命中了断点。所以我们可以检查 `$arg1` 来看看接收者是什么，看起来还是相同的高度标签，但现在值是“0 feet”。我看到了我策略的问题。

当宇航员跳跃时，代码会实时更新高度标签，所以断点会很频繁地命中这个对象，这会花我们很长时间。要针对“尝试次数”标签命中 `-[UILabel setText:]` 的断点会非常困难。所以我认为我应该做的是，只在跳跃动画完成后才设置这个符号断点，让我给你们展示一种方法。我将切换到断点导航器。如果我双击符号断点的指示器，我们可以把编辑器调回来，然后使用这个条件字段。我们可以在这里输入一个返回 true 或 false 的表达式，只有当该表达式求值为 true 时，断点才会触发。如果我们有一个像 jumpAnimationInProgress 这样的属性，我们可以编辑一个表达式来测试它是否为 false，这样断点就会触发。这种情况下我没有属性，所以我会向你们展示另一种方法。我将删除那个符号断点，而是向下滚动到这个函数 jumpCompleted 并在这里设置一个断点。

jumpCompleted 是在动画完成后被调用的，这样它可以更新 UI 和游戏状态。然而，我们不想在这个函数中中断。我想要做的是配置这个断点，让它为我设置 `-[UILabel setText:]` 上的符号断点。我可以添加一个调试器命令动作来实现，即 `breakpoint set`，并且我将使用选项 `--one-shot true`。一次性断点是一个临时断点，只在触发前存在，触发后会自动删除。

我们可以给它符号名称 `-[UILabel setText:]`，并将其设置为一个自动继续的断点。所以在这种情况下，我们创建的是一个断点，当执行进入 jumpCompleted 函数时，它会在我们感兴趣的位置设置一个临时断点，然后继续。这样我们只会在流经这个函数之后才命中那个 setText 断点。

所以我们按继续，我们看到模拟器中的跳跃动画完成了，现在我们命中了 `-[UILabel setText:]` 上的断点。现在我们可以通过 `po $arg1` 来看看那个消息的接收者。我们看到这确实是一个不同的 UILabel 实例，值为 0，所以它很可能是顶部这些标签之一。所以我们认为找到了正确的对象，让我们看看修改这个标签值的代码。

我们可以通过在选择调试导航器中的堆栈的上一个帧来做到这一点。现在我们找到了修改标签值的代码。

它当前正在使用 labelText 变量传入一个值为“0”的字符串，往上查看，我们可以看到 labelText 始终被设置为标签的当前值，所以它不会改变。看起来 valueText 是包含新值的变量，所以可能只是一个拼写错误；让我们修复它。

让我们把它改成 valueText。

然后，我想做的，而不是重新编译并重新运行来测试这个修改，我想像之前一样，在当前运行的 App 的上下文中实际测试这个修改。所以我将在当前行下方添加一个断点，因为记住，我们修改了代码但没有重新编译。所以当标签仍然会被设置时，我们将在下面添加我们的代码行，把它设置为我们认为应该的值。又是一个自定义断点，我们可以再次使用 expression 来注入那段代码，并使其自动继续。

所以现在，如果我按继续，代码执行将继续流过这里，我们看到尝试标签确实更新了。我想确认一下那也有效。嗯，谢谢。

我想确认在成功跳跃后对于分数标签也有效，所以让我们导航回这里。我可以移除这个创建一次性断点的 jumpCompleted 断点，因为我们不再需要它了。我可以禁用 jump 中的断点，因为我们不再想修改 didReachSelectedHeight。现在，当我点击宇航员时，他执行了一次成功的跳跃，我们可以看到所有标签都正确更新了，所以这个错误对我来说看起来很好。

让我们返回并勾掉它。

好了。下一个错误是游戏结束状态的问题。游戏应该在 10 次尝试后结束，所以我可以点击宇航员，等待动画播放，并在游戏中前进以到达那个状态并尝试重现它。但是动画需要一些时间，在测试和验证修复时我可能需要多次这样做，所以我想跳过所有的跳跃动画。让我向你们展示我将如何做到这一点。我将导航到 updateUIForJumpSucceeded，我们可以看到这个函数修改了一些颜色，然后调用了 `jumpAstronautAnimated(true)`。所以看起来我只需要调用 `jumpAstronautAnimated(false)`。我可以修改代码并重新编译，但就像我之前说的，如果可能的话，我不喜欢为了调试而修改代码，所以让我展示我将使用的替代技术。我将在这行上设置一个断点。让我们清空调试控制台，通过点击宇航员发起一次新的跳跃，现在我们暂停在这一行。

所以我需要让调试器用对 `jumpAstronautAnimated(false)` 的调用来替换这一行。嗯，代码已经编译进去了，我们不能替换它，但我们可以做的是让调试器跳过这一行，不执行它，而是跳过它。然后我们可以使用 expression 来注入我们想要做的修改或者调用。

那么我们如何跳过一行代码呢？让我把你们的注意力引到这个绿色的注释标签“thread 1”上。我们称它为指令指针。它指向包含下一条将要执行指令的那一行。而这个图标，看起来有点像拖拽手柄，实际上，它就是一个拖拽手柄。如果我按住鼠标不放，我可以移动它，我能够在暂停时改变指令指针，所以我可以把它向下移动一行，然后松开，这时我们会从 Xcode 得到一个吓人的消息。基本上，它是在说，能力越大，责任越大。老实说，这是今天我要告诉你们的最有风险的功能。但它有风险只是因为调试器允许你把指令指针移动到你喜欢的任何地方。它并不在乎，但它不能保证应用程序的状态将保持完整。所以，例如，如果你最终引用了一个尚未初始化的对象，或者过度释放了一个对象，你可能导致内存管理问题。

但我们现在都来听了高级调试讲座，所以我们知道自己在做什么。让我们按下蓝色按钮。

好的。我们已经跳过了那一行，现在在控制台中，我们可以使用 expression 并调用 `jumpAstronautAnimated(false)`。

现在我们按继续，看看这一切是否有效，确实，游戏状态更新了，我们跳过了所有的跳跃动画。

我希望每次我点击宇航员时都发生这种情况，所以我要在这里配置这个断点来为我做这件事。

首先，我们需要一个能跳过一行的调试器动作，对应的命令是 `thread jump`。

我给它选项 `--by 1`，这告诉调试器为当前线程跳过一行代码。然后我们只需要调用我们的 expression，我们可以通过按加号按钮并添加另一个命令动作来实现。

`expression jumpAstronautAnimated(false)`。并使其自动继续。

所以我们这里得到的是一个断点，当执行到达这一行，但在它执行这一行之前，断点被触发。它将执行命令来跳过那一行，然后使用 expression 来调用我们想要替代执行的函数调用。所以现在，如果我们点击宇航员，我们可以快速地推进游戏状态，跳过所有动画，并轻松地重现我们的错误。

所以，正如我所说，游戏应该在 10 次尝试后结束，而我们已经远远超过那个次数了，所以让我们看看游戏状态。

这些全都存储在上面一个叫 gamePlay 的属性里，所以我将在那个属性上设置一个断点，并开始一次新的跳跃。现在我们暂停在对该属性的下一次引用处。

我将使用 po 来查看该对象的当前状态，这里我们看到了这个 GamePlay 对象的调试描述。我们这里有一个自定义的调试描述，所以值得指出的是，po 会请求对象的编程调试描述，你可以自定义这些描述。我将向你们展示我们是如何为 GamePlay 做到这一点的。如果我切换到源代码并滚动到底部，你可以看到我们添加了一个扩展，使 GamePlay 遵循 CustomDebugStringConvertible。

遵循该协议要求你实现一个名为 debugDescription 的属性并返回一个字符串。你可以返回任何你想要的字符串来表示这个对象，用于你的调试目的。

你也可以通过实现 `debugDescription` 为 Objective-C 对象做同样的事情。

将其与命令 `p gamePlay` 进行比较。

`p` 是另一个 LLDB 命令，它使用 LLDB 内置的格式化器来表示对象。所以在这里，我们看到了同一个对象的两种表示，默认的格式化器会向你显示完整的类型名称、内存地址，然后是所有属性及其值的列表。所以我们在这里可以看到有一个 maxAttempts 属性，它正确地设置为 10，所以看起来可能有一个逻辑错误，在 attempts 增加后，它没有成功地判断出已经超过了最大值。所以我想找到修改 attempts 的代码，看看逻辑是什么样的。我将打开变量视图（Variables view）并展开视图控制器以查看其所有属性，然后在底部，我将使用过滤器输入“gamePlay”来找到那个属性。展开它的属性，然后我将选择 attempts 属性。

在这里，我将打开上下文菜单并选择“Watch “attempts””。

现在这样做会创建一个所谓的监视点（Watchpoint）。

在断点导航器中，在所有断点下方，你会看到有一个名为“Watchpoints”的新分组，我们有一个用于 attempts 的监视点。监视点就像一个断点，但它会在下次该变量的值被改变时暂停调试器，所以我们可以移除这个属性断点，因为我们不再需要它了，然后按继续。现在我们暂停在这个监视点，我们找到了修改 attempts 变量的代码。我可以禁用这个监视点，因为我不再需要它了。我们可以在游戏运行时查看这里的代码：`incrementAttempts`，如果成功，则 `incrementScore`。所以我没有看到任何逻辑来检测 attempts 是否已超过最大值并过渡到游戏结束状态。

所以我认为这就是需要的全部，但在这种情况下，我想在实际修改代码之前先测试我的假设。所以我会创建一个断点并配置它来注入那个修改，看看在我修改代码之前它是否能解决问题。所以再次，我可以添加一个带有 expression 的调试器命令动作，我认为我们需要的是：如果 attempts 大于等于 maxAttempts，那么我们将游戏状态更改为 ended，并使其自动继续。

所以现在很容易通过按继续来测试这是否真的解决了问题。执行将继续流过这个断点。注入代码，我们可以看到它确实看起来解决了问题。我想从游戏开始就验证这一点，我可以快速简单地通过再次点击“播放”并快速进行 10 次尝试来做到这一点。在第十次，我们看到它确实检测到了游戏结束状态，所以看起来这就是我们需要的修复。

现在，别忘了把它应用到你的代码中。所以我只是复制出来，拖拽断点删除它。然后我可以粘贴进去，看起来不错。

让我们勾掉这个，这个部分只剩下一个错误了，那就是尝试和分数标签的布局。现在，这个应用程序的布局已经留给了工程师们，就像优秀的工程师们所做的那样，我们已经找到了一个高效的位置，塞在顶部角落。但团队认为这样不太合适，所以他们把它打回来了，要求我们重新尝试。所以我想为这些分数标签模拟一个新的布局。现在我可以拿出我的图形应用程序开始模拟，但我是一名工程师，我喜欢用代码来模拟。事实上，我是一名调试器工程师，所以我喜欢使用带有实时应用程序和真实数据的调试器来模拟。让我向你们展示如何做到这一点。

让我们导航回去，在 jump 函数中设置一个断点。我们需要做的第一件事是找到一个我们可以摆弄的视图的引用，所以我将清除所有东西并打开它，然后开始一次新的跳跃。我们暂停在视图控制器中的这个 jump 函数里。

当然，如果你有一个视图的属性或插座变量，那是一个很好的引用。但如果没有，那么你需要获取一个视图的内存地址。让我向你们展示一些查找内存地址的方法，以及如何仅通过内存地址来操控一个视图。

嗯，就像我们之前说过的，调试描述包含一个自定义描述。所以查看视图控制器的视图，我们可以看到 UIView 的默认调试描述包含了视图的类名和内存地址。所以一种方法就是直接获取对象的调试描述。对于这个来说很容易，因为它有一个属性。但是如何获取视图控制器视图下的所有视图呢？嗯，我们需要查看视图层级结构，一种方法是使用这里的这个按钮，它会调用 Xcode 的视觉视图调试器。它将快照视图层级结构并给你一个 3D 展开视图，你可以用那个来以这种方式检查视图。

Sebastian 几分钟后会更多讲到这一点，所以让我向你们展示另一种方法，它适用于较简单的视图层级结构，并且能让你留在调试控制台中。那就是使用 UIView 上一个叫做 `recursiveDescription` 的调试函数。

所以我们应该能够调用 `po self.view.recursiveDescription()`。

然而，这不起作用。为什么呢？嗯，recursiveDescription 仅用于调试目的。它不是公共 API 的一部分，因此对 Swift 不可见。Swift 是一种严格的语言，不允许你调用未明确定义的函数。

然而，Objective-C 代码在 Objective-C 世界可以自由地运行，你几乎可以做任何你想做的事。我的意思是，它是一种动态语言，所以你可以调用这样的函数。所以我们需要做的是告诉调试器用 Objective-C 语法来求值这个表达式。实现这一点的方法是使用带有选项 `-l objc` 的 expression。这会告诉 expression，即使你在 Swift 帧中，你将要给它的是 Objective-C 代码。我们还会给它 `-O`，告诉它我们也想要调试描述，就像 po 所做的那样，以及 `--` 来表示没有更多选项了。

该行的其余部分只是原始的表达式输入。所以我们应该能够给它这个方法调用的 Objective-C 格式。

不幸的是，这不太奏效，原因是 expression 会为 Objective-C 编译创建一个临时的表达式上下文，它并不会继承来自 Swift 帧的所有变量。不过，有一种方法可以绕过去。

如果我们只是把 `view` 放在反引号中。反引号就像一个步骤，它说首先，在当前帧中求值这些内容并插入结果，然后我们可以求值其余部分。现在我们得到了递归描述。

使用这个，我们可以看到所有视图的所有调试描述。我对承载这些标签的记分板视图感兴趣，所以我们可以找到其中一个的内存地址。现在我们可以使用 `po 内存地址`，如果你是一名 Objective-C 开发者，你可能对此很熟悉。嗯，那不起作用，这是因为 Swift 不会把数字当作指针并为你解引用。所以我们再次需要从 Objective-C 上下文中执行此操作。所以我们可以像之前那样做，但我发现这非常方便，以至于我想把它缩短成一个简单简短的命令。所以我将通过使用 `command alias` 来做到这一点，我将把这个命令叫做 `poc`。

所以现在我已经创建了一个别名，我可以简单地使用 `poc 那个内存地址` 来查看该对象的调试描述。如果只有一个内存地址，我想向你们展示另一种查看对象描述的方法。在 Swift 中，你可以使用一个名为 `unsafeBitCast` 的函数。

给它内存地址，然后它是不安全的，因为需要你提供正确的类型，所以我会给它 `ScoreboardView.self`。

现在我们看到我们可以使用 `unsafeBitCast` 来查看一个对象的调试描述。

`unsafeBitCast` 很棒的一点是它返回一个类型化的结果，所以我们可以调用它的函数和属性名，比如 `.frame`。

在这种情况下，我想检查一个中心点，然后修改那个中心点。让我们把它改成 300，我们可以看到它已经变成了 300，但模拟器中的视图并没有移动。嗯，为什么没有？嗯，我们暂停在调试器中，所以 Core Animation 目前没有将任何视图修改应用于屏幕的帧缓冲区。但我们可以要求 Core Animation 为我们做这件事，只需使用表达式 `CATransaction.flush()`，这会告诉 Core Animation 更新屏幕的帧缓冲区。

所以现在，我可以只用这两行来修正新的位置，继续刷新，我们就可以移动了。

实际上，我发现这非常方便，以至于我有点想把这些都包装到一个单一的、可以移动视图的命令中，这就是我所做的。

让我向你们展示。我将切换到终端并打开一个 Python 文件。

为什么是 Python 文件？嗯，LLDB 是可以用 Python 编写脚本的，你可以完全访问 LLDB API。

所以我创建了一个 LLDB Python 脚本来创建一个 nudge 命令，它接受一个 x 偏移量、一个 y 偏移量和视图表达式，你可以使用它在调试器暂停时移动视图。虽然看起来像是一个很长的脚本，但大部分是参数解析。它的核心部分，在中间，只是调用我们手动调用的表达式。

我们很遗憾没有时间深入讨论这个脚本。但我们会把这个脚本提供给你们下载，这样你们就能看到它是如何工作的，并将其用作自定义调试命令的基础。

让我向你们展示如何启用这样的脚本。只需在你的主目录中编辑 `.lldbinit` 文件，并添加一行 `command script import`。

我还想添加一些我觉得方便的别名，比如我之前创建的 `poc` 别名，以及用于刷新事务的别名。我想我会记住这一个。

我将复制 `command script import`，这样我们可以直接粘贴到调试会话中，以省去重新启动会话的麻烦。现在我们有了一个叫 `nudge` 的命令。所以我可以，比如说，水平 nudge 0，垂直 nudge -5，给它那个视图的内存地址，然后开始在模拟器中移动它。

LLDB 很棒的一点是，如果你在空白行上按回车，它会重复上一行，所以这对于微调来说非常棒。

我可以把它向右移动一点，让它刚好合适。然后让我们处理另一个视图。我们可以给它任何视图表达式，比如把 attemptsView 放到这里。nudge 的另一个特性是，一旦你给它一个视图表达式，你就不必重复那个表达式。

它会记住它，并将其应用于你之前指定的同一个视图。像这样的看起来不错。比以前我们有的布局要好。我现在可以做的，是获取 nudge 提供的信息，比如应用于该视图相对于其原始中心点的总偏移量，以及你的 frame 值。回到我的代码，修改我的布局代码或自动布局约束，这样我就为我的场景轻松模拟了一个新的布局。

现在最后一件事——嗯，首先，别忘了勾掉“debug”，这非常重要。

然后在重启或重新编译并重新运行之前要做的最后一件事，是禁用或移除任何正在注入表达式的断点，因为你不希望那些代码行被执行两次。

简单地选择它们或一组它们，然后按删除键，是删除这些断点的快速方法。

这些就是我喜欢用来增强调试工作流程的一些调试技术。

请注意，我们是如何在不重新编译或重新运行的情况下诊断并修复了所有四个错误的。

这可以节省大量时间，特别是对于复杂项目，并且在尝试解决难以重现的错误时至关重要。谢谢你们，希望你们喜欢这些内容，并能在你们的调试过程中使用这些技术。

我想快速回顾一下我们在那次调试会话中涉及的所有功能和技巧。首先，我们看到了如何使用 Xcode 行为来专门用一个标签页进行调试，以及如何使用 LLDB 表达式来修改程序状态。

我们可以使用带有调试器命令的自动继续断点来动态注入代码，并且我们可以使用 `breakpoint set --one-shot` 作为另一个断点的调试器命令动作来创建依赖断点配置。

即使在汇编帧中，我们也可以使用 `po $arg1`、`$arg2` 等轻松检查函数参数，并且我们可以通过拖拽指令指针或使用命令 `thread jump` 来跳过代码行。

我们可以使用监视点请求调试器在变量被修改时暂停，甚至可以在 Swift 帧中使用 `expression -l objc` 来求值 Objective-C 代码。我们可以使用表达式 `CATransaction.flush()` 请求将视图更改直接刷新到屏幕上，即使在调试器暂停时也是如此。

你可以添加自定义的 LLDB 命令，无论是将常用命令别名为更快捷的快捷键，还是使用 LLDB 的 Python 脚本功能完全自定义并创建你自己的命令。别忘了查看我们的讲座网站。我们将很快发布那个 nudge 脚本，所以你可以下载它，查看它，并将其用作你命令的基础。

还有一件事我想和你们谈谈，那就是当前的 LLDB 打印命令。你可能熟悉 `po`。我们在演示中大量使用了它，我们看到了 `po` 请求对象的调试描述，并且你可以自定义它。这是因为 `po` 只是 `expression --object-description` 或 `expression -O` 的一个别名，相比之下，`p` 命令只是 `expression` 的一个别名。它使用 LLDB 的内置格式化器来显示该对象的表示形式。

第三个需要了解的重要命令是 `frame variable`。它与前两个命令的不同之处在于，它根本不需要编译和求值表达式。它只是直接从内存中读取变量的值，然后使用 LLDB 的内置格式化器。

所以选择使用哪个命令更多是个人偏好，以及你在调试时想看到的信息类型。

但要记住很重要的一点是，如果你最终遇到表达式失败，或者 po 和 p 对你不起作用的情况，如果你需要检查当前帧中的变量，那么 `frame variable` 应该仍然可用。

说到这里，我想把时间交给 Sebastian，他将向你们介绍一些高级视图调试技术。谢谢。

谢谢，Chris。

我很兴奋地想向你们展示一些技巧和窍门，让你们充分利用 Xcode 的视图调试器。我们还将看看在 Xcode 10 中我们做的增强功能，以便在你采用 macOS 的深色外观时提供良好的调试体验。我们将在演示中看到这一点。

让我切换到演示机，我将使用 Chris 一直用的同一个项目，你们已经看到我们还有两个错误要解决。不过，我不会使用 iOS App，我会使用 Mac App。

所以我们在这里可以看到 Solar System App 的 Mac 版本，我们可以看到它在深色模式下看起来相当不错，但我们今天要解决两个错误。

首先，行星图像没有在水平方向正确居中，这是一个非常明显的错误。你可以看到，在右侧，这个地球图像移到了右侧，所以我们将研究这个问题。第二个错误是，弹出框（popover）中的描述在深色模式下无法阅读。让我告诉你那是指什么。

当我切换到这款应用时，我可以在这个弹出框中调出轨道详细信息。

你可以看到顶部的标签清晰可读；然而，底部的标签很难阅读，我真的不得不选中文本才能阅读它。

所以这就是我们要看的两个错误。

让我隐藏待办事项列表，让我们直接开始。我想要做的是用 Xcode 捕获这个应用的视图层级结构，然后检查它。

我们会找到问题，然后希望能找到修复方法，这样我们都能去喝啤酒。

问题是，当我切换到 Xcode 来捕获视图层级结构时，这个弹出框会因为应用进入后台而被关闭，我们将无法捕获它的视图层级结构。

所以我们必须在其活动状态下捕获该应用，我将向你们展示两种方法。

你可以看到，当我现在切换到 Xcode 时，这个弹出框是如何被关闭的。

首先，我们可以使用触控栏，我通过从 Xcode 的“窗口”菜单中调出触控栏模拟器来向你们展示它是什么样的。

让我切换回 Solar System App，再次调出弹出框。看一下触控栏，你可以看到有一个喷雾罐图标，当我在触控栏上点击它时，你可以看到 Xcode 在其调试栏中提供的调试选项的子集。所以这是一种从触控栏访问这些选项的非常方便的方式。正如你所看到的，即使 Xcode 在后台，我也可以调出它们，所以即使你以全屏模式开发你的应用，你也可以访问它们。

其中一个选项允许我捕获视图层级结构。我现在不打算这样做，因为我知道不是每个人都有触控栏，所以我会向你展示另一种方法。

我将关闭模拟器，我将使用按住 Command 键点击的方式来点击 Xcode 调试栏中的按钮。

Command 键点击是一个系统级手势，允许你执行鼠标事件而不激活该鼠标事件所作用于的应用程序。

所以这允许我们调用视图层级结构的捕获。调试器在应用程序仍处于活动状态时暂停了它，我们可以看到 UI 仍然在渲染，就好像应用是前台应用一样，并且弹出框没有被关闭。如果你想知道为什么会出现那个旋转的图标，那是因为应用程序在调试器中暂停了，不再响应鼠标事件。

现在，你可能会想，如果我们查看这里的视图调试器，为什么弹出框不可见。

别担心，视图层级结构已经被捕获了。等我讲到那个错误时，我会讲到我们如何查看弹出框，但首先我想看看这个图像视图的布局问题。所以我在这里选择图像视图，并放大一点。当我们查看右侧的检查器时，我们可以看到这是一个 `_NSImageView` 简单图像视图。这个对象带有下划线前缀的事实通常暗示着它是一个来自系统框架的内部类，而不是我们在代码或 Interface Builder 中设置图像视图时使用的类。所以让我们在视图层级结构中查看这个对象。我可以使用“导航”菜单，选择“在调试导航器中显示”来做到这一点。

现在，在左侧，我们可以相对于它的父视图和子视图看到它。

现在，我们可以看到它的父视图实际上是一个 NSImageView，所以这就是我们正在寻找的。

我们还可以看到它的父视图是一个 PlanetGlobeView，而 PlanetGlobeView 的父视图是一个 NSEffectView。

所以我将在这里选择图像视图，我们现在也可以在右侧看到图像视图的其他属性。

让我们检查一下这个视图的布局。

我在这个应用中使用自动布局，所以我想看一下自动布局约束。

我可以使用这里的这个按钮来显示约束，我们现在可以看到当前影响此视图布局的所有约束。你可以看到，例如，这里有一个约束。

我们也可以看到这个垂直线，这是一个对齐约束。

当我选择这个约束时，我们可以在右侧的检查器中看到所有属性。如果你想知道为什么视图调试器现在只显示线框，那是因为它只显示当前参与所选视图布局的那些视图的内容。由于所有这些视图本身没有内容，所以我们目前只显示线框。

所以，选中该约束后，让我们看看右侧的检查器。

我们可以看到它将图像视图的水平中心与 PlanetGlobeView 的水平中心对齐，并且常量为零。所以它在其父视图中水平居中。

现在让我们从调试导航器中选择 PlanetGlobeView，我们可以看到它向左稍微大一点，但它是右对齐的。这有点奇怪，因为我们刚刚看到约束将它们正确地或完全水平地对齐，但这并不是我们在视图调试器中看到的。所以让我们看看 PlanetGlobeView 的约束，看看我们是否能理解发生了什么。我在这里选择前导约束，再看看检查器，我们可以看到它将 PlanetGlobeView 的前沿与我们在父视图中看到的 NSVisualEffectView 的前沿对齐。所以它只是相对于其父视图插入它，并且常量为 30，所以那说得通。

而这里的尾部约束将 PlanetGlobeView 的后沿与父视图的后沿对齐，常量也是 30。

这个约束在右侧没有连接到任何东西的事实有点可疑，让我怀疑我们是否看到了整个画面。在这种情况下，通常最好检查一下是否有任何内容被默认情况下看不到的裁剪。

你可以使用下面的这个按钮来显示裁剪的内容。现在，当我启用这个功能时，你可以看到 PlanetGlobeView 实际上延伸到了窗口边界之外，向右侧延伸，现在水平居中约束又有意义了，因为它确实在其父视图中正确地居中。然而，父视图延伸到了窗口之外。这是一个非常常见的问题，如果你在代码中设置约束，你不小心交换了第一个和第二个项，从而导致布局方向错误；或者你意外地颠倒了常量，在这种情况下，我们使用了 30 而不是使用负 30 来向左插入。所以我想做的是尝试一下修复，即颠倒常量。我将使用 Chris 之前介绍的相同技术，通过 LLDB 来应用它。

所以，选中这个约束后，我选择“编辑”，然后“复制”。

我将调出底部的控制台区域，这个复制操作给我的是选中对象的指针，这适用于你在视图调试器以及内存图调试器中选择的所有对象。这使得在控制台中使用它们非常方便。所以让我们——谢谢。

让我们打印调试描述，我们可以确认常量确实是正 30。

我们在检查器中看到的也是这个。所以让我们将常量设置为负 30。我将输入 `e`，这是 expression 的超短形式，通过转换指针，然后调用 `setConstant(-30)`。我们遇到了与 Chris 之前看到的相同的路径应用不更新的问题，所以我将要做，或者说我必须做的是，必须通知 Core Animation 安排更新其 UI。我可以使用 Chris 之前添加的便捷命令，所以我可以从这里获取命令。

现在我们可以看到行星图像水平居中正确了，所以颠倒常量是正确的修复。所以我们能够确认这一点，现在让我们在代码中应用这个修改。

选中约束后，你可以看到在右侧我可以看到这里的回溯信息，这就是该约束的分配回溯，它精确地告诉我在哪个帧中它被分配了。所以它允许我直接跳转到我创建该约束的代码处。

要让这些创建回溯显示出来，你必须启用 Malloc 日志记录，让我向你们展示如何启用它。你去到上面的方案，然后编辑方案。

在方案选项的诊断标签页中，在“日志记录”下，你可以启用“记录所有分配的历史 Malloc 栈”。并且请确保勾选“记录所有分配的历史 Malloc 栈”。

这将在视图调试器以及 Xcode 的内存图调试器中为你提供所有这些方便的、针对所有选定对象的分配回溯。

现在，当我把鼠标悬停在这个栈帧上时，我们可以看到该帧的完整名称。

我们可以看到它是 SceneViewController 中的 `setupPlanetGlobeViewLayout` 方法。

我可以使用这个“跳转到”箭头跳转到代码中的这些点。

我将通过按住 Shift-Control-Option 键来做到这一点，这会带出这个导航界面，并允许我在一个单独的窗口中打开这个文件。

现在你可以看到分配该约束的代码行被高亮显示。

我们可以看到常量为 30，我可以将其颠倒为负 30。

我将保存这个文件并关闭它，我们完成了第一个错误。

完美。所以第二个错误是我们无法读取那个弹出框内的描述，所以让我们看看那个。

首先，我想禁用约束模式和裁剪，这样我们又能看到常量了。我还会清除控制台。

现在，我在开始的时候向你们展示了如何在其活动状态下捕获它，这样我们就能在弹出框打开时获取它的视图层级结构；然而，我们没有看到它。那是因为视图调试器一次只向你显示一个窗口。

让我向你们展示如何查看其他窗口。

当我在视图层级结构中向上滚动，并向上遍历视图层级结构，最终到达我们看到的当前窗口的窗口时，我们可以看到它由一个窗口控制器托管。如果我在这里折叠这个根级别项，我们可以看到实际上还有另一个根级别项，这正是我们正在寻找的。

我们的弹出框。

所以如果你的应用程序有多个窗口，macOS 和 iOS 都是如此，它们在左侧的大纲中会显示为多个根级别对象。

所以去那里看看，如果你认为你的应用程序应该有多个被视图调试器捕获的窗口。

我们可以用 3D 方式查看它，我们可以看到有一个大视图遮挡了对标签的点击。我想看看这些标签，以便检查它们。有一个技巧可以在视图调试器中点击穿透视图。你可以简单地按住 Command 键。

所以我可以点击穿透前面的这个视图，并选择这个蓝色标签。

让我们看看这个标签的文本颜色。我想先看看那些在深色模式下看起来不错的标签，这样我们希望能为底部有问题的标签推导出一个解决方案。让我们看看文本颜色，我们可以看到这是一个 RGB 颜色，结果是这个蓝色，我们还可以看到检查器为这个颜色提供了一个名称。这表明这个颜色来自我们项目中的资产目录（Asset Catalog），在 Xcode 10 中，你可以为你定义的一种颜色提供多种变体。例如，你可以为亮色提供一种，为深色提供一种。

选择哪种变体由视图的外观决定，你可以在检查器中获得该信息。我在这里向下滚动到检查器中的“视图”部分。你可以看到有“外观”和“有效外观”。现在这个视图上没有显式设置“外观”。这是一个非常常见的场景，因为大多数视图从某个父视图、窗口或应用程序继承外观。

但你可以在这里看到继承的“有效外观”。这就是 vibrant dark，所以它决定了在你的资产目录中定义的颜色的哪种变体被选中。

好的。实际上，当我在检查器中向下滚动到这里时，我想指出 `description` 属性，它是该对象的调试描述。Chris 之前向你们展示了如何为你的对象提供自定义调试描述。所以，当你使用 po 作用于对象时，你不仅在控制台中受益于提供良好的调试描述，而且你还能在视图调试器中受益，因为它会直接显示在检查器中。

好的，让我们回到文本颜色。我想选择第二个标签，因为它在深色模式下看起来也很好。在这种情况下，我们可以看到它有一个命名颜色。它是 labelColor，但它带有 `system` 前缀。这表明它并非来自我们自己的资产目录，而是来自系统，当然，系统颜色也会自动适应外观变化。

现在，看看这里有问题的标签，我们可以看到它的颜色是非常深的灰色，而且没有名称。这意味着它是一种自定义颜色，不会适应外观变化。

所以我们想要做的是改变它的颜色，它的文本颜色，改为系统颜色。所以，选中这个对象后，我按 Command-C 复制。

我输入 `e`，粘贴指针，然后说 `.setTextColor(NSColor.textColor)`。

再次，我必须进行一次事务刷新。

我们可以看到弹出框现在更新了，字体清晰可读。

现在，我不会在我的 storyboard 文件中应用这个修复。

但我想指出的一点是，当你做出修改后，确保你的应用程序在所有外观下看起来都良好非常重要，因为现在有多种系统外观。

我将向你们展示如何做到这一点。我将继续运行，而不是将我的整个系统外观切换到亮色来查看标签在亮色背景上是否仍然好看，Xcode 10 为你提供了一种仅为目标应用覆盖外观的方法。你可以使用调试栏中的这个按钮。我可以在这里选择“浅色”，你可以看到应用程序现在以浅色外观呈现。

我可以调出弹出框，我们可以验证文本清晰可读。

所以这确认我们已经解决了问题。现在，由于在不同的外观下查看你的应用程序是一个非常常见的操作，我们实际上使该选项在触控栏中可用。我将向你们展示。我将再次调出触控栏模拟器，并且弹出框打开后，我可以在这里选择这个选项。

现在你可以在触控栏中看到所有覆盖选项，所以即使你的应用处于全屏模式，例如，我也可以访问这些。

让我切换到“高对比度浅色”，它启用了高对比度的辅助功能特性，并将外观覆盖为浅色，这样你也可以确保你的应用在该配置下看起来也很好。

当然，我也可以回到系统外观。所以我们确认我们的问题已经修复。这允许我在我们的错误列表上勾掉这个。至此，我的演示部分结束，我将回到幻灯片。

让我们回顾一下我们刚才看到的。

我向你们展示了如何使用“在调试导航器中显示”来在左侧的层级大纲中定位你当前的选择。

我向你们展示了如何显示裁剪的内容，并且我们使用了自动布局调试功能来追踪我们的约束问题。我向你们展示了如何使用对象指针，你可以通过简单地复制选中的对象轻松访问它们，并在 LLDB 中使用它们。我们查看了创建回溯，如果你在方案选项中启用了 Malloc 栈日志记录，这些回溯就可用，以便直接跳转到代码处并应用我们为约束所需的更改。

我们查看了视图调试器检查器中方便可用的调试描述。

我们查看了用于选择位于另一个视图后面的视图的点击穿透技巧。关于调试深色模式，我们看到你可以直接从 Xcode 的调试栏或触控栏轻松覆盖目标应用的外观。

我向你们展示了如何捕获活动状态下的 Mac 应用。我们查看了现在在 Xcode 的视图调试器检查器中可用的命名颜色信息及其外观信息。如果你想了解更多关于在你的 Mac 应用中适配深色模式的信息，请查看这两个讲座的视频。

我们这次演讲到此结束。

如果你想获得关于这次讲座的更多信息，包括 Chris 之前向你展示的脚本，它将会发布在讲座网站上。如果你对本次讲座的任何内容或者一般的调试有疑问，明天上午 9 点有一个性能分析和调试实验室。Chris 和我都会在那里。我们很乐意回答你可能有的任何问题。还有一个关于 iOS 内存深度剖析的讲座，如果你感兴趣的话，那也是关于内存调试的，也在明天。

说到这里，希望你们在 Beer Bash 上玩得开心，并享受会议的剩余部分。

[ 掌声 ]
