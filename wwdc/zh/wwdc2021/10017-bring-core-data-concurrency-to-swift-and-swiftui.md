---
title: 将 Core Data 并发引入 Swift 和 SwiftUI
session_id: 10017
collection: wwdc2021
year: 2021
duration: '27:04'
topics: [Swift, 'SwiftUI & UI Frameworks', System Services]
group: C · 并发、锁与线程
evergreen: true
source_url: 'https://developer.apple.com/videos/play/wwdc2021/10017/'
content_hash: 'sha256:cd2e6fd80f32a5ec'
translated: true
---

# 将 Core Data 并发引入 Swift 和 SwiftUI

<sub>WWDC2021 · 27:04 · Swift、SwiftUI & UI Frameworks、System Services</sub>

了解 Core Data 如何采用 Swift 5.5 的新并发功能，从而让你的代码更简洁、高效且安全……

> [!note] 归档理由
> Core Data 的并发上下文模型

## 相关资源

- [加载和显示大型数据馈送](https://developer.apple.com/documentation/SwiftUI/loading-and-displaying-a-large-data-feed)
- [高清视频](https://devstreaming-cdn.apple.com/videos/wwdc/2021/10017/4/F075C8AE-C8D2-40F9-8520-018581E3A771/downloads/wwdc2021-10017_hd.mp4?dl=1)
- [标清视频](https://devstreaming-cdn.apple.com/videos/wwdc/2021/10017/4/F075C8AE-C8D2-40F9-8520-018581E3A771/downloads/wwdc2021-10017_sd.mp4?dl=1)
- [构建通过 CloudKit 和 Core Data 共享数据的 App](https://developer.apple.com/videos/play/wwdc2021/10015)
- [认识 Swift 中的 async/await](https://developer.apple.com/videos/play/wwdc2021/10132)
- [在“聚焦”中展示 App 数据](https://developer.apple.com/videos/play/wwdc2021/10098)
- [来而复往：Apple Watch 上的数据传输](https://developer.apple.com/videos/play/wwdc2021/10003)
- [SwiftUI 新变化](https://developer.apple.com/videos/play/wwdc2021/10018)
- [FetchRequest 动态配置：排序描述符](https://developer.apple.com/videos/play/wwdc2021/10017/?time=1236)
- [FetchRequest 动态配置：谓词](https://developer.apple.com/videos/play/wwdc2021/10017/?time=1293)
- [SectionedFetchRequest](https://developer.apple.com/videos/play/wwdc2021/10017/?time=1406)
- [SectionedFetchRequest 动态配置：排序描述符](https://developer.apple.com/videos/play/wwdc2021/10017/?time=1496)

## 逐字稿

> [!warning] 关于逐字稿
> 这份逐字稿是 Apple 的自动语音识别产物，**未经人工校对**，可能有术语转写错误。段落已按原始 HTML 的 `<p>` 结构重组，但断句仍可能不自然。

♪ ♪ 大家好。我是 Core Data 团队的 Michael LeHew。今天，我非常兴奋地想和大家谈谈团队所做的一些改进，这些改进让 Core Data 与 Swift 的结合使用体验变得真正出色。首先，我会回顾 Core Data 如何出色地满足所有 Apple 平台上用户的数据持久化需求。接着，我会讨论 Core Data 如何采用 Swift 运行时中新的并发机遇。然后，我会介绍我们对 Core Data API 的一些增强，让你的 Swift 代码更具表现力。最后，我会通过探索 Core Data 为我们于 2020 年引入的现有 SwiftUI 支持添加的动态功能来结束本次讨论。不过，让我们从头开始——无论你正在为哪个 Apple 平台开发，你只是拥有一个 App。在某个时刻，你的 App 终将遇到用户数据，并且你可能需要将数据存储在某处。一个很好的选择就是使用 Core Data。对于希望以强大且功能丰富的方式持久化用户数据的 App 开发者来说，Core Data 是 Apple 的标志性框架。该框架负责处理恰当管理用户数据的许多复杂性，从数据在内存中作为对象图的呈现方式，到数据在存储中的建模方式。该框架还付出巨大努力来管理重要的运行时考量因素，例如内存使用和延迟。该框架提供的功能也是可扩展的。你可以从一个简单的本地持久化存储开始，扩展到使用多个执行上下文以提高性能，甚至可以通过 CloudKit 创建强大的共享数据体验。Core Data 也适用于所有 Apple 支持的平台。我真的很想强调最后这一点。一旦你开始使用 Core Data，你所学到的一切都将适用于我们的每个平台，从你的 Mac，到你的 iPhone，甚至你的 Apple Watch。

当然，Core Data 在 Swift 中也能很好地工作。在过去的几年里，我们一直持续增强 Core Data API，使其在 Swift 中尽可能富有表现力。对于 Swift 来说，今年是相当令人兴奋的一年，因为语言和运行时中引入了全新的并发功能。自诞生之初，Core Data 就一直关注并发运行代码，这背后有充分的理由。持久化数据需要对外部存储介质进行读取和写入，这使得支持新的并发模型成为自然而然的事情。让我们在 Earthquakes 示例 App 的上下文中探讨这是如何工作的。这个 App 从美国地质调查局（USGS）读取数据馈送，并使用 Core Data 存储有关近期地震的信息，例如震级、位置和发生日期。在架构上，Earthquakes 是一个 Swift App，它有一个视图上下文来驱动 UI，还有一个后台上下文来接收 USGS 提供的数据。我们的示例为我们的 App 提供了一个本地容器，并从 USGS 的 JSON 馈送中收集地震数据。

在我们下载数据时，会将其交给 JSON 解析器，然后导入到后台上下文中，转换为托管对象并保存到本地存储。视图上下文随后合并这些更改，神奇地更新我们的 UI。在 2020 年，我们专注于如何通过批处理操作高效处理这些数据。但现在，我想把重点放在我们如何并发执行这些操作上。具体来说，我想关注我们将数据导入 App 所采取的三个步骤。第一步是下载原始数据，下载成功后，App 需要将其转换为我们特定的本地表示形式。最后，将新对象保存到持久化存储中。让我们把这个过程转换为高级代码。我将每个操作抽象为各自的函数或闭包。App 首先从服务器获取原始数据，处理成方便的本地表示形式，然后通过对后台托管对象上下文执行批处理插入请求，将对象导入 Core Data。

像这样编写代码，可视化潜在的瓶颈会稍微容易一些。通过网络加载数据是考虑异步执行工作的绝佳时机。转换也可能是一个需要考虑的地方。此外，将数据导入持久化存储似乎也非常合适。

然而，从历史上看，在所有这些情况下，你都需要自己实现任何此类异步机制，或者严重依赖于特定框架的实现。我们来谈谈 Core Data 的特定抽象。

在 Core Data 中，当你调用 `performAndWait` 时，托管对象上下文会在其自己的受保护执行环境中执行提供的闭包。这可能会占用调用线程，直到工作完成。

如果我们将其可视化，可以想象三个代码块，我分别标记为 BEFORE、DURING 和 AFTER。

当我们的代码运行时，首先标记为 BEFORE 的代码有机会在发起线程上执行。然后我们调用 `performAndWait`，调用线程将阻塞，直到在 DURING 闭包期间完成的工作结束。当该工作完成后，标记为 AFTER 的代码将执行。

当然，如果你不需要等待闭包完成，我们一直提供完全异步的变体。但今年，Swift 获得了强大的并发模型和深度语言集成，这使得 Core Data 能够更准确地描述我们 API 的意图。语法略有不同。你需要使用 `await` 来等待 `perform` 的结果，但使用这个新 API 的心智模型与托管对象一直支持的完全相同。然而，好处在于，并发不再是隐藏的实现细节，而是深度集成到了 Swift 语言中。正因为如此，编译器可以自动防止许多常见的并发错误，例如数据争用和死锁，甚至在已知任务正在等待结果时高效地利用资源。让我们回到代码，看看实际使用是怎样的。

正如我们所见，你需要使用 `await` 一个声明为 `async` 的函数。这有可能挂起调用执行上下文，直到异步函数通过返回控制权来交出控制。

它还能与 Swift 现有的结构化错误处理无缝协作，将所有抛出的错误路由到调用帧，正如你所期望的那样。现在，我们已经看到了一个调用异步函数的例子，让我们看看它们是如何声明的，来看看 Core Data 全新的在托管对象上下文内执行异步工作的方法。这一小段 Swift 代码中封装了相当多的功能，但我只想谈谈几个重要的细节，然后我们将跳转到演示如何在实践中使用它。从新的 `perform` 重载的声明开始。你可以看到它是基于它可以返回的结果类型而泛型的，并且带有新的 `async` 关键字，这使该函数具备了 Swift 中新并发功能的特性。也许这个新 API 最重要的方面是，提供的闭包现在允许你抛出错误或返回值，从而省去了手动将这些信息传回调用帧的麻烦。让我们通过探索几个不同的场景来看看这有多酷。从历史上看，由于并发隐藏在我们的实现内部，将错误传出 `performAndWait` 的唯一方法之一就是通过闭包捕获一个可选值，然后在事后检查它。如果你使用的是完全异步版本的 `perform`，情况可能会更复杂，因为你需要做大量的管道工作，到处传递完成处理程序，并确保一致地使用它们。使用 Swift 新的并发模型，所有这些管道工作都为你处理好了！只需使用 `try await` 你的异步工作，如果发生错误，就抛出它，事情就会自然地展开到调用帧。

所以，现在我们关注了错误，但结果呢？嗯，我描述的一切工作方式完全相同。让我们看一个具体的例子。

在跳进代码之前，让我们先勾勒出我们想做的事。对于这个例子，我想配置一个获取请求，以识别过去五小时内发生的地震次数。作为一句话，这是一个直截了当的任务描述。但在代码中，我们需要稍微调整一下顺序。

我们首先需要弄清楚五小时前是什么时候，为此我们可以使用 `Calendar` API 以健壮的方式计算出来。然后，我们将配置一个带有基于该日期的谓词的获取请求，并要求返回计数结果类型。在代码中，它看起来与我们的计划非常相似。我们使用 `Calendar` 的偏移量 API 计算当前时间之前的五小时，然后配置一个 `Quake` 的 `FetchRequest`，返回计数结果，并带有一个匹配我们关心日期的谓词。从历史上看，返回结果遵循与捕获错误类似的模式。你通过闭包捕获任何需要改变的状态，在托管对象上下文中执行计算，然后在重新获得控制权后使用结果。

现在，我们可以简单地使用 `try await` 我们的 `perform` 调用的结果，并将结果直接从 `perform` 返回给我们的调用帧。其余的代码完全相同。我们只是避免了手动值传递以及那段代码可能存在的任何潜在错误或细微差别。这段新代码非常简洁且富有表现力。

然而，值得一提的是，有些时候你应该谨慎行事。让我们看另一个例子来了解原因。这个例子试图将最近的地震作为托管对象返回。虽然新的 API 使得返回值变得非常容易，但返回已经注册到托管对象上下文的托管对象是不安全的。只有在 `perform` 调用的闭包内引用这样的已注册对象才是有效的。相反，如果你需要在不同的执行上下文之间引用托管对象，可以使用对象 ID 并根据需要重新获取，或者使用获取请求的字典表示选项。在再看一个例子之前，我想先介绍一个我还没有谈到的细节。这个细节就是 `ScheduledTaskType`。到目前为止，我们看到的所有异步 `perform` 都是基于此选项的默认值：`.immediate`。还有第二个选项叫做 `.enqueued`。要理解这两种调度方式之间的区别，有助于思考当你请求调度工作时，在托管对象上下文内部具体发生了什么。正如我们所见，`.immediate` 的行为很像 Swift 异步感知版本的 `performAndWait`。如果你在不同的执行上下文上运行，并请求等待在后台上下文上执行的工作，你将一直等到它被调度并完成。

然而，如果你已经在同一个执行上下文中，工作会立即被乐观地调度。

另一方面，`.enqueued` 则更简单一些。它只是简单地将请求的工作追加到上下文的工作集的末尾，无论发起调用站点的关联性如何。让我们再看一个例子。所有这些异步特性你也可以采用。在这里，我将我们一直在讨论的导入逻辑重构为一个新的 `importQuakes` 函数，并带有新的 `async` 关键字。这个函数反过来又通过其他异步功能来实现。

现在，任何人都可以 `await` 这个新函数，以利用 Swift 中的新并发特性。让我们总结一下到目前为止我们所看到的。总而言之，这个新 API 将 Swift 结构化并发的支持直接带入了 Core Data。新的 `perform` API 变体只是你所熟知和喜爱的现有 Core Data API 的 Swift 并发感知版本。我们强烈建议你在应用程序中利用这个新 API。

此外，`NSManagedObjectContext` 并不是 Core Data 中唯一支持在其受保护并发域内执行任务的类型。我们还为 `NSPersistentContainer` 和 `NSPersistentStoreCoordinator` 添加了类似的 API。这些 API 的整体形态和行为与我描述过的非常相似。但是，拥有了所有这些并发能力，如果我不建议你利用现有的调试工具，那就是我的失职了。当然，Xcode 提供的 Address Sanitizer 和 Thread Sanitizer 对于捕捉你可能甚至不知道存在的错误非常有帮助。这两个工具都可以在 Scheme 编辑器的 Run 设置的 Diagnostics 面板中找到。每个 Sanitizer 检测不同类型的问题，包括验证安全内存使用假设以及跨多个线程适当使用数据。在向你的用户社区发布软件之前，总是建议同时使用这两种 Sanitizer 来鉴定你的应用程序及其相关的测试。

虽然这些 Sanitizer 在所有上下文中都很有用，但我也想强调，Core Data 提供了一个特殊的运行时标志，你可以启用它以获得更多特定领域的帮助。通过启用此选项，Core Data 将打开许多有用的断言，以验证内部锁并确认各种 Core Data 类型的正确使用。

采用 Swift 并发支持并不是今年对 Core Data 的唯一改变。我们引入的每一个新 API，从 CloudKit 共享到新的“聚焦”集成，都是考虑到其在 Swift 中的呈现而精心设计的。今年，我们为这些主题分别设有专场，我鼓励你去看一看。此外，我们还在整个框架中进行了全面检查，以确定可以在 Swift 中做出改进的其他地方，现在我想向你们展示其中的一些，从我们支持的不同类型的持久化存储开始。回想一下，持久化存储描述了你希望如何物理存储用户的数据。Core Data 目前提供四种这样的存储：XML、二进制、内存和 SQLite。你一直使用这些标识符。今年，我们继续前进，在 Swift 中为它们提供了更自然的名称。现有的名称将继续有效，但使用这些名称的新 API 使用起来将更加符合人体工学，因为名称更短并且能够自动补全这些符号。当然，持久化存储并不是 Core Data 中唯一关注类型的方面。毕竟，该框架就是关于存储类型化数据的，而这些类型是通过属性描述来描述的。

今年，我们为属性描述添加了一个新的可扩展枚举，为处理其类型提供了更自然的语法。让我们通过编写一个单元测试来看看它们是如何运行的，这个测试可以验证我们的运行时模型是否与我们在 Xcode 模型编辑器中设计的相匹配。

为简单起见，我们将尝试验证由我们的地震对象模型定义的单个运行时类型，但你可以想象这如何扩展。这似乎是一个很小的测试，但验证它是件好事，因为它可以加快未来更有趣的诊断速度。为了编写这个测试，我们将根据新的属性类型编写一个快速的辅助函数。让我们现在描述一下这个函数。我们从签名开始，它期望一个属性名称、我们关心的实体描述，以及用新的 `AttributeType` 枚举描述的类型。

这个工具函数的定义相当简单。我们首先验证我们有一个具有提供名称的属性，如果找不到则测试失败。然后我们验证属性的类型是否符合预期。就是这样。我们可以为每个实体和属性重复此操作，并放心我们的运行时行为符合我们定义的模型。

这只是我们在今年为 Core Data 枚举在 Swift 中进行的一些人体工学改进的示例。

到目前为止，我一直关注许多底层的框架交互以及它们在 Swift 中的表现形式，但如何向用户呈现数据呢？在 2020 年，我们引入了一些在 SwiftUI 中使用 Core Data 的便捷功能。现在，我的同事 Scott 有很多关于我们今年引入的新增强功能要与大家分享。Scott？谢谢，Michael！今年在使用 Core Data 与 SwiftUI 的体验方面有一系列改进，首先是获取请求中的懒加载实体解析，这放宽了 App 在构造视图之前就需要设置好 Core Data 堆栈的要求。同样在今年，获取请求为其排序描述符和谓词支持动态配置。

还有一种新型的获取请求支持分区获取。我将使用 Michael 在演讲中提到的 Earthquakes 示例 App 逐一介绍这些功能，首先是懒加载实体解析。

可能在你的 App 中，有一些像这样的代码。这里的 `container` 属性对于支持此类型中的代码，甚至更广泛的 App 来说，并不是真正必需的。所有这些东西都直接从 `QuakesProvider` 类型获取它们所需的东西。不，这个属性的存在是为了确保在任何视图在环境尝试引用任何实体、在模型加载之前，Core Data 堆栈已经设置好。请看这里，环境视图修饰符是在 `ContentView` 初始化之后调用的。当部署到今年的 SDK 时，不再需要这个技巧了。`FetchRequest` 属性包装器现在在获取时按名称懒加载查找实体，此时环境已保证 Core Data 堆栈已设置好，所以现在可以安全地删除这个属性……

然后直接引用……

环境调用中的 `QuakesProvider` 共享容器。继续介绍一些新的 API，`FetchRequest` 现在支持动态配置。被包装值上有两个新属性，用于直接更改请求的谓词及其排序描述符，这些排序描述符既可以用你熟悉的 `NSSortDescriptor` 表示，也可以用新的 `SortDescriptor` 值类型表示，后者在使用自动生成的托管对象子类获取实体时提供更多便利性和安全性。

最后，还有一个配置绑定，它具有与被包装值相同的属性集，以便于与视图集成。在这个新 API 之前，我不得不设计我的视图，使得排序和谓词参数通过视图的初始化器传递，但这使得支持诸如使用工具栏中的控件配置我的获取请求之类的事情变得非常棘手。这些新的动态配置属性消除了这种摩擦，我很乐意向你展示如何通过向 Earthquakes 示例 App 添加排序和过滤来使用它们。我们先来看排序描述符。默认情况下，Earthquakes App 按时间进行排序，但我也想按震级排序，所以我要添加一个菜单，让我控制结果的顺序。

我将从添加一个静态元组数组开始……

包含我想要支持的排序描述符以及它们的名称。请看这里，它们也使用了新的 `SortDescriptor` 类型。

我还需要一点状态来跟踪我当前正在使用的排序顺序。我已经为此创建了一个类型，所以我将其添加为 `ContentView` 的一个属性。现在，我向列表视图添加一个工具栏菜单……

它会修改选定的排序，以及一个更新获取请求排序描述符的 `onChange` 修饰符。

现在在预览中，我们可以看到新的菜单，我可以使用它按震级对地震进行排序。很好！现在来添加过滤。我想根据地震的位置进行过滤。我首先需要的是搜索文本框文本的一些状态。我将创建一个绑定属性……

用于搜索文本框，以更新获取请求。

有了这些，我只需要 UI。方便的是……

`searchable` 需要一个字符串的绑定，所以我们可以直接把它放在这里。

现在在我们的预览中，我们可以通过在这个新字段中输入 "sandwich" 来缩小到附近所有与三明治匹配的地震。

这就是 `FetchRequest` 的动态配置。另一个常见请求的功能是对分区获取的支持，它今年作为一种名为 `SectionedFetchRequest` 的新属性包装器类型出现。这个类型支持与 `FetchRequest` 相同的新动态配置属性，但它用一个额外的参数初始化，一个标识分区的属性的键路径，很像 `NSFetchedResultsController`。

但与 `NSFetchedResultsController` 不同的是，标识分区的属性可以是任何你想要的类型，只要它是 `Hashable` 的。这通过 `SectionedFetchRequest` 上的一个额外泛型参数在类型系统中进行编码。最后，这个新类型包装了一个二维结果类型。`SectionedFetchResults` 是分区的集合，每个分区本身又是结果的集合。每个分区还有一个带有分区标识符的属性。

这很容易采用，所以我将向 Earthquakes App 添加分区获取。首先，我更新我的 `FetchRequest` 声明。

`Quake` 已经有一个 `day` 属性，所以我将使用它作为分区键路径。

接下来，我需要更新 `body` 属性……

以匹配新的分区结果类型。

外层的循环在这里遍历分区，所以我在这里发出一个 `Section` 视图，每个分区本身是 `Quake` 的集合，所以这个内层的 `ForEach` 遍历该分区，就像我之前遍历结果一样。

如果我们看一下预览，我现在已经按时间排序并按天分区的地震。而且 SwiftUI 甚至为我提供了折叠分区的自动支持。

这个新的 `SectionedFetchRequest` 类型支持与 `FetchRequest` 相同的动态配置属性，以及一个用于分区标识符键路径的额外配置属性。这非常重要，因为改变排序实际上不再安全。它可能导致分区不连续，因为时间和地震震级并不完全相关，这可能是件好事。为了解决这个问题，我需要更新上面的排序……

使得每个排序都有对应的分区标识符键路径。

接下来，在工具栏中……

我需要每次更新排序描述符时也更新分区标识符键路径。

但这里是关键部分。对请求的更改在结果 getter 被调用时提交，所以为了安全地同时更新排序和分区……

我需要在一个我已提取到本地变量的对结果的引用上更新配置。

现在在预览中，我们可以看到更改顺序也会更改分区。我们可以在按时间排序、按天分区，以及按震级排序和分区的地震之间切换。

就这样：懒加载堆栈初始化、动态配置和分区获取，所有这些都可以轻松地应用到使用 iOS 15 和 macOS Monterey 的现有 App 上。

所以，总结一下，Core Data 是你在所有 Apple 平台上管理 App 数据持久化需求的一站式解决方案。它通过一个新的 `perform` API 利用了 Swift 中可用的新并发特性，并且仍然内置了强大的线程安全调试功能。

它拥有新的枚举接口，使得存储和属性类型在 Swift 中使用起来更加自然，此外还有 CloudKit 共享和“聚焦”集成。而且，通过动态配置和分区获取，使用 SwiftUI 将数据连接到视图比以往任何时候都更容易。

还有很多与此相关的其他新内容可以学习。我们建议查看集合 "Simplify with SwiftUI" 和 "Meet Swift Concurrency"。就是这样！我非常期待看到你们用这些新 API 构建出什么样的作品。[轻快的音乐声]
