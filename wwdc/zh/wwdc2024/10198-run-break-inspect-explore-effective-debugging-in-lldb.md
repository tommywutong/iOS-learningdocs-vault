---
title: '运行、中断、检查：探索 LLDB 高效调试'
session_id: 10198
collection: wwdc2024
year: 2024
duration: '28:48'
topics: [Swift, Developer Tools]
group: G · 调试、崩溃与 Instruments
evergreen: true
source_url: 'https://developer.apple.com/videos/play/wwdc2024/10198/'
content_hash: 'sha256:80d7538a6ebdefc4'
translated: true
---

# 运行、中断、检查：探索 LLDB 高效调试

<sub>WWDC2024 · 28:48 · Swift、Developer Tools</sub>

学习如何使用 LLDB 探索和调试代码库。我们将向你展示如何充分利用崩溃日志和回溯，以及如何...

> [!note] 归档理由
> LLDB 现代工作流

## 章节

- [简介](/videos/play/wwdc2024/10198/?time=0)
- [议程](/videos/play/wwdc2024/10198/?time=42)
- [将调试视为搜索问题](/videos/play/wwdc2024/10198/?time=75)
- [崩溃日志与启动程序](/videos/play/wwdc2024/10198/?time=247)
- [断点](/videos/play/wwdc2024/10198/?time=447)
- [断点动作](/videos/play/wwdc2024/10198/?time=730)
- [help 命令](/videos/play/wwdc2024/10198/?time=927)
- [高频触发的断点](/videos/play/wwdc2024/10198/?time=965)
- [p 命令](/videos/play/wwdc2024/10198/?time=1164)
- [@DebugDescription 宏](/videos/play/wwdc2024/10198/?time=1539)
- [总结](/videos/play/wwdc2024/10198/?time=1670)

## 相关资源

- [简介](https://developer.apple.com/videos/play/wwdc2024/10198/?time=0)
- [议程](https://developer.apple.com/videos/play/wwdc2024/10198/?time=42)
- [将调试视为搜索问题](https://developer.apple.com/videos/play/wwdc2024/10198/?time=75)
- [崩溃日志与启动程序](https://developer.apple.com/videos/play/wwdc2024/10198/?time=247)
- [断点](https://developer.apple.com/videos/play/wwdc2024/10198/?time=447)
- [断点动作](https://developer.apple.com/videos/play/wwdc2024/10198/?time=730)
- [help 命令](https://developer.apple.com/videos/play/wwdc2024/10198/?time=927)
- [高频触发的断点](https://developer.apple.com/videos/play/wwdc2024/10198/?time=965)
- [p 命令](https://developer.apple.com/videos/play/wwdc2024/10198/?time=1164)
- [@DebugDescription 宏](https://developer.apple.com/videos/play/wwdc2024/10198/?time=1539)
- [总结](https://developer.apple.com/videos/play/wwdc2024/10198/?time=1670)
- [LLDB 调试器](https://lldb.llvm.org/)
- [论坛：Developer Tools & Services](https://developer.apple.com/forums/topics/developer-tools-and-services?cid=vf-a-0010)
- [高清视频](https://devstreaming-cdn.apple.com/videos/wwdc/2024/10198/4/A6D919A0-000B-4A54-AE83-6F261757D780/downloads/wwdc2024-10198_hd.mp4?dl=1)
- [标清视频](https://devstreaming-cdn.apple.com/videos/wwdc/2024/10198/4/A6D919A0-000B-4A54-AE83-6F261757D780/downloads/wwdc2024-10198_sd.mp4?dl=1)
- [使用结构化日志进行调试](https://developer.apple.com/videos/play/wwdc2023/10226)
- [符号化：进阶技巧](https://developer.apple.com/videos/play/wwdc2021/10211)
- [WatchLater 按钮](https://developer.apple.com/videos/play/wwdc2024/10198/?time=489)
- [打印 Watch Later 列表信息](https://developer.apple.com/videos/play/wwdc2024/10198/?time=774)
- [断点动作：打印最近添加视频的名称](https://developer.apple.com/videos/play/wwdc2024/10198/?time=825)
- [断点动作：在命令行中](https://developer.apple.com/videos/play/wwdc2024/10198/?time=882)
- [@DebugDescription 宏示例](https://developer.apple.com/videos/play/wwdc2024/10198/?time=1606)

## 逐字稿

> [!warning] 关于逐字稿
> 这份逐字稿是 Apple 的自动语音识别产物，**未经人工校对**，可能有术语转写错误。段落已按原始 HTML 的 `<p>` 结构重组，但断句仍可能不自然。

大家好，我叫 Felipe，是 Apple 调试技术团队的一名工程师。在本演讲中，我们将介绍一些调试技巧，帮助你轻松探索代码并更快地发现错误。LLDB 是 Xcode 内置的底层调试器，能够随时暂停程序、检查变量状态、计算表达式等等。在这次演讲中，我们将介绍 LLDB 提供的主要工具，同时也会展示你可能不熟悉的新功能和高级技巧。我们将首先定义一个调试模型来指导我们使用调试器。然后，我们将介绍一种使用崩溃日志进行调试的不同方式。我们将探索使用断点（breakpoint）暂停程序执行的不同方法，以及某些编码模式如何与调试器交互。接着，我们将介绍检查程序状态的主要工具：`p` 命令。最后，我们将介绍 Swift 6 的一项新功能，它允许我们自定义数据类型在调试器中的显示方式。在调试程序问题时，我们通常会得到一个时间点，在该点程序表现异常。这可能是崩溃、显示了错误的值，甚至是程序挂起。在程序执行的起点与观察到异常行为的点之间，有错误的代码被执行了。我们的目标是找到那段代码。通常可以通过检查程序在不同时间点的状态来找到错误，每次检查都使我们更接近问题代码。我们可能熟悉几种检查程序状态的不同技巧。例如，一些代码库使用日志语句。读取日志文件中的一条记录，就像在某个时间点检查程序状态一样。如果日志足够详细，我们可能只需要这些就能定位代码中的缺陷。这需要程序员有预见性地确定哪些信息值得记录，但对于在用户和开发者之间传输 App 诊断信息来说，这是一种强大的技术。另一种常用的技巧是 print 调试，这大概是我们所有人学习的第一个调试方法。使用 print 调试时，我们在程序中插入 print 语句，重新编译代码，运行程序，然后重现错误。最后，我们检查打印出的消息。如果需要打印新的内容，就重复整个过程。最终，我们将检查足够的程序状态来修复错误。但我们最好不要忘记在调试完成后删除这些 print 语句，因为我们都有过 print 语句误入生产环境的趣事。整个过程可能非常耗时且容易出错。在这次演讲中，我们将展示如何通过使用调试器更快地遍历这个搜索空间。为此，我们将介绍 LLDB 提供的主要工具：回溯（backtrace）、变量检查、断点和表达式计算。我们还将展示 LLDB 如何帮助你在不运行程序的情况下调查问题。使用调试器时，我们始终在重复三个不同的动作：运行程序、在感兴趣的点中断、以及检查程序状态。检查程序后，我们可以继续执行到程序后面的某个点，或者，如果需要检查之前的时间点，我们可以重新启动程序。高效地重复运行、中断、检查循环是有效调试会话的关键。让我们把它付诸实践！大多数调试会话从编译代码并在调试器下运行 App 开始。很多时候，我们只需要在 Xcode 中点击启动按钮，或者通过命令行使用目标可执行文件及其参数启动 LLDB。然而，调试问题的第一步是能够重现错误；LLDB 也可以在这方面提供帮助，使用一种甚至不需要启动 App 的技巧。

在 Apple 平台上，每当程序崩溃时，系统会收集崩溃时程序状态的信息并创建崩溃日志。LLDB 能够读取崩溃日志并将其以类似调试会话的形式呈现，允许开发者对崩溃发生的原因进行初步调查。有时，这可能就是我们找出错误根源所需的一切。我有一个同事发给我的崩溃日志，他正在测试 Destination Video，一个跨平台的视频播放 App。让我们用 LLDB 打开它！要打开崩溃日志，可以右键点击文件并用 Xcode 打开。Xcode 会询问是否要在项目上下文中打开该文件。让我们选择 Destination Video。

现在，Xcode 使用 LLDB 创建了一个包含崩溃时程序状态的调试会话。崩溃的行被高亮显示，告诉我们代码无法打开一个 JSON 文件。就在崩溃之前，程序记录了它试图打开的文件名，所以我可以联系我的同事索取他们的日志文件。但程序是如何到达这个点的呢？调试器提供了一个工具来回答这个问题：回溯（backtrace）。回溯描述了导致这个程序状态的函数调用序列，或栈帧（stack frame）。它提供了每个函数在做什么、它们在哪里被调用、以及它们各自将返回到哪里的视图。

我们可以在 Xcode 的调试导航器中找到当前的回溯。那么程序崩溃时正在做什么呢？当前帧是一个 JSON 加载函数。更早的一个帧显示正在导入视频元数据。而这发生在程序初始化期间。

回溯是理解程序控制流（control flow）的强大工具，无论是在崩溃日志中还是在常规调试会话中。当与崩溃日志结合使用时，回溯可以帮助我们建立关于崩溃是如何发生的直觉。在我们的例子中，崩溃日志还帮助我们识别了程序可能已记录的信息，为调查问题提供了另一条途径。要获得崩溃日志正确的行号信息，请确保项目检出在生成该崩溃日志的 App 版本对应的同一提交上，并且该构建的 dSYM bundle 可用。我们在《符号化：进阶技巧》中更详细地介绍了 dSYM bundle。

Destination Video 是用 SwiftUI 构建的，我正试图了解更多关于它的知识，所以我原型设计了一个功能，允许用户将视频添加到“Watch Later”列表中。我的目标是了解某些代码行何时被执行，让我们探索一下 LLDB 和断点如何帮助实现这一点。从 App 的主屏幕，如果我们选择一个视频，会进入一个展示其内容的 DetailView。

在这个视图中，我们可以找到我创建的“Add to Watch Later”按钮，当我点击它时，其文本会发生变化。

为了帮助我理解它是如何工作的，我将在创建该按钮的地方设置一个断点。这是我原型设计的代码，调用了 Button 类的构造函数；让我们点击相关的行号在那里设置一个断点。

我们的新断点现在显示在断点导航器中。但请注意，一旦我们启动 App 会发生什么：LLDB 已经将该行断点解析为三个独立的位置。

这表明我们可能通过不同的代码路径停在这个断点处。让我们通过再次导航到一个视频的 DetailView 来验证这个假设：调试器停止了程序。Xcode 高亮显示了程序停止的行，它即将调用 Button 的构造函数。通过检查回溯，我们可以确认我们正处在构建 UI 元素的嵌套调用之中。

例如，这个帧创建了一个元素垂直叠放。让我们看看上一个帧包含什么。

这一个正在创建一个 ScrollView。

我们已经停在一个断点处，但 LLDB 已经识别出与此行关联的总共三个位置。我们可以使用 `breakpoint list` 命令获取更多关于它们的信息。它描述了我们设置在 70 行的断点，但也使用行号和列号描述了与该断点关联的三个位置。列表中的第一个位置是程序当前停止的地方：第 70 行对 Button 构造函数的调用。我们可以从行和列信息中观察到这一点，但我们也可以使用断点标识符，或 ID。

LLDB 为每个断点位置分配一个 ID，在本例中是 1.1。这也是 Xcode 高亮显示断点行时使用的相同标识符。列表中的第二个断点，标识符为 1.2，指的是构造函数的第一个参数，即动作闭包（action closure）。一旦我们点击按钮，这个断点应该会被触发。最后一个断点位置，其标识符为 1.3，指的是构造函数调用中的尾随闭包（trailing closure）。这个位置实际上被解析到下一行，尽管闭包体从上一行的花括号开始。让我们尝试到达这些断点！从这个初始构造函数调用开始，让我们继续执行。

程序在尾随闭包内停止，在断点 1.3 处。使用回溯，我们发现构造函数是更早的一个帧：换句话说，是构造函数本身调用了这个闭包。

为了到达最后一个断点，我们必须点击“Add to watch later”。

我们再次停止了，但这次我们在动作闭包内部！这个例子说明了即使是最基本的断点也可能导致以有趣的方式中断。我们有三个独立的代码区域归属于同一行，但通过不同的代码路径到达：对构造函数的调用，构造函数自身调用的尾随闭包，以及仅在按钮被点击时才被调用的动作闭包。这是大量使用闭包的声明式代码中的常见场景，比如 SwiftUI。我们并不总是知道闭包何时会被调用，所以在闭包体内设置一个行断点是在它被调用时暂停的好方法。暂停 App 是调试循环的重要组成部分，但我们可以通过将其与程序检查相结合来改善调试体验。例如，让我们尝试了解 UI 元素如何与程序交互，并专注于第一个断点，即调用 Button 构造函数的那一个。为了仅当按钮被创建时中断，让我们禁用最后两个断点位置。

现在让我们点击按钮，这应该会触发 UI 更新和我们的断点。

使用 LLDB，我们可以通过 `p` 命令检查 Watch Later 列表的大小，我们将在后面更详细地探讨这个命令。

列表中有一个视频，就是我们刚刚添加的那个。我们甚至可以检查它的标题。

在我们的断点/检查调试循环中，一遍又一遍地重复相同的命令可能会很繁琐。利用断点动作的概念，调试器可以通过在到达断点时运行命令来帮助我们。让我们更改我们的示例，以便在每次命中断点时打印 Watch Later 列表的条目！我们可以通过右键点击断点来找到编辑断点菜单：让我们添加一个调试器命令动作，如果存在的话，打印列表中最近视频的名称：我们甚至可以在命中断点后继续执行。

现在每次调用构造函数时，我们都能获得队列的信息！这是一种利用调试器打印信息而无需重新编译代码的方法。到目前为止，我们一直在使用 Xcode 的图形用户界面与调试器交互，但 LLDB 也提供了丰富的命令行界面。让我们重复上一个例子的步骤，但这次使用命令行。要访问调试器命令行，我们首先暂停 App：现在我们可以使用 `b` 命令设置一个断点，它是更通用的 `breakpoint set` 命令的简短替代。

命令行也可以用来添加断点动作，但请注意，这会覆盖通过 Xcode 设置的任何动作。让我们使用“break command add”来打印添加到 Watch Later 列表的最后一个视频的名称：和之前一样，这会在打印后继续执行。此命令影响最近的断点，但如果提供了可选的断点标识符参数，它也可以修改不同的断点。

LLDB 通过使用 help 命令提供其所有命令的详细描述。你也可以获取特定命令任何选项的帮助。要发现更多 LLDB 功能，一个好用的命令是 apropos，它搜索 LLDB 的帮助文本以查找关键字，并返回由该关键字描述的任何命令或选项。例如，搜索与回溯相关的命令会找到 `frame select` 及其别名 `f` 命令。它还会找到 `thread backtrace` 命令。调试时，我们经常设置一个被多次触发的断点，但我们只对其中的一部分感兴趣。一个常见的例子是当断点被放置在循环内时，我们不想在每次迭代都中断，而只想在某些事件发生时中断。让我们回顾一下可用于处理高频触发断点的三种主要技术。考虑这段代码片段，它遍历集合中的视频，如果视频位于远程位置则加载它们，然后处理它们。我们可能只对在视频非常长时停在 `loadRemoteMedia` 函数处感兴趣。我们可以通过设置一个行断点并用断点条件修改它来实现这一点，条件定义了调试器是否应该停止程序的规则。在命令行中，我们会使用 `break modify` 命令，为其提供断点 ID 和一个条件。任何在断点位置有效的代码都可以用作条件表达式。在我们的示例中，我们可以修改断点，使其仅在当前视频时长超过 60 秒时停止程序。在 Xcode 中，我们可以右键点击断点，导航到编辑断点，并填写条件字段。

回到我们的示例，我们可能只对在 `processVideo` 调用之前也执行了 `loadRemoteMedia` 函数的情况感兴趣。在这种情况下，我们可以再次设置一个行断点，但这次添加一个断点动作。我们之前使用过断点动作来打印变量，但它们也可以用来创建新的断点。使用 `tbreak` 命令，我们可以创建一个临时断点，它只导致程序在该位置停止一次。在我们的示例中，我们可以在 `loadRemoteMedia` 上设置一个自动继续的断点，其动作是在 `processVideo` 上创建一个临时断点。

第三种技术包括忽略断点固定次数，并在后续命中时停止。例如，我们可以忽略集合中的前十个视频。为此，我们将像之前一样修改断点，但现在使用 `--ignore-count` 标志。使用 Xcode，在编辑断点界面中也可以使用相同的选项。在极端情况下，当一行代码被执行数百万次时，之前的技术可能会显著减慢程序执行速度，因为调试器仍然需要每次都停止并决定是继续还是停止。这种情况建议重新编译代码。例如，我们可以计算我们的停止条件，并在一个仅在条件为真时才执行的 `if` 语句内设置断点。一个巧妙的小技巧是使用带有 `SIGSTOP` 信号的 `raise` 函数：这会指示 App 停止，如果你是通过 Xcode 或 LLDB 运行它，调试器将接管，就好像命中了断点一样。到目前为止，我们专注于调试周期的两个组成部分：启动程序和在感兴趣的位置中断。但我们只是粗略地看了一下检查程序状态的主要工具：`p` 命令。在前面的例子中，我们将 `p` 命令作为查看变量和计算表达式的主要方式。LLDB 还提供了许多其他命令来实现这一点，它们各有用途。理解所有这些工具可能会让人不知所措，但自 Xcode 15 以来，在大多数你需要检查变量或计算表达式的情况下，`p` 是正确的命令。它已被重新设计为“做我想做的事”（do what I mean）打印命令的别名，通过将许多不同的工具整合到一个命令下，可以节省你的时间。我们在《使用结构化日志进行调试》中对此进行了详细介绍。让我们试试看！我现在正尝试向 App 中添加一个新视频，为此我编辑了视频的 JSON 描述。然而，我一启动 App，它就崩溃了。

这是我编辑过的包含新视频的 JSON 文件。让我们启动 App，以便调试器可以在崩溃发生时停止。

通过检查控制台，我们注意到 DestinationVideo 的开发者在他们的工作流中使用了日志记录。

记录的最后一条信息是正在加载视频 JSON 文件。

从程序启动到报告异常之间出了点问题。此时，一个好的首选是在调用 JSON 加载函数之前设置一个断点，看看是否能让我们更接近错误。让我们试试看。

通过右键点击日志消息，我们可以快速导航到相应的源代码位置。

让我们在这个函数的末尾设置一个断点。

由于我们需要在更早的时间点中断，我们将重新启动程序。因为没有修改代码，我们可以使用 Control + 点击来重新启动而无需重新编译，以节省时间。

让我们看看 URL 和文件名这两个局部变量：看起来都没问题。我们也可以在变量查看器中查看它们。

或者简单地在源代码上悬停查看。

有些类型甚至有一个快速查看按钮，为变量提供更多详细信息。

这看起来就是我编辑过的文件。

我们已经检查了 JSON 加载函数，看起来是正确的，所以错误可能就在那之后的某个地方。让我们看看程序的另一个部分：接受 JSON 解码器的 Video 构造函数。

我怀疑这些 `try` 语句之一有问题，但如果我们在每次调用 Video 构造函数时都中断，我们会遇到一个问题：App 里有太多视频了！幸好，我们介绍了可以处理像这样的高频触发断点的技巧。我知道我的新视频是 App 中的第 12 个视频，所以我可以对这断点应用忽略次数。不过，让我们尝试点新的：Swift 错误断点。这种断点指示 LLDB 在 Swift Error 被抛出时立即停止 App。

让我们点击继续！程序在我们的错误断点处停止了，当时正试图解码 `imageName` JSON 键。那里有些不对劲。让我们使用回溯向上移动到更早的帧，那里有输入数据。

作为一名程序员，我喜欢用编码的方式来处理这类情况，而 `p` 命令给了我很大的自由。让我们编写一些代码来找出这个 `data` 数组中有多少个 `imageName`。

哎呀，我们需要先创建一个字符串。

输出太多了。让我们搜索 `imageName`。

更近了，但输出还是太多。让我们检查一下 `count` 属性。

啊，我预期有 13 个视频，但我只找到 12 个名为 `imageName` 的键。出问题了。让我们看看我们的 JSON 文件。

啊，我在写 `imageName` 时打错了一个字！让我修复它。

搞定了！通过这个例子，我们探索了 `p` 命令如何检查变量以及计算复杂表达式；它可以在回溯的任何帧中做到这一点。我们能够逐步构建一个复杂的表达式，在每一步打印中间结果，而无需重新编译任何代码。这次调试会话还让我们了解到如何通过包含 JSON 文件中哪个键缺失的信息来增强 App 的日志。到目前为止，我们检查的大多数变量都相当简单。然而，包含过多数据的类型在调试会话中检查起来很麻烦，如果能在变量查看器或 `p` 命令中有一个简短描述会更好。这些类型通常有许多属性，或者经常存储在集合中。例如，变量查看器中的 `WatchLaterItem` 集合不会透露每个条目的任何信息，除非我们手动展开它们。LLDB 一直提供一种机制来自定义 `p` 和变量查看器的输出。Swift 6 引入了一种直接从源代码中使用新的 `@DebugDescription` 宏实现此目的的机制。除了使用此宏注解类型外，我们还必须创建一个总结该类型的 `debugDescription` 字符串属性。它必须使用字符串插值和存储属性创建。让我们为 `WatchLaterItem` 类型实现这一点！我们的数据结构包含三个相关的数据成员：一个 `video`、一个 `name` 以及它被添加到列表的日期。我们首先用 `@DebugDescription` 宏标记该类型。然后，我们创建一个 `debugDescription` 字符串计算属性。在这种情况下，我们将使用 `name` 和 `addedOn` 日期作为该类型的摘要。现在，如果我们再次检查变量查看器，摘要会显示在集合的每个项目中！如果你之前使用过 `CustomDebugStringConvertible` 协议，这个例子可能看起来眼熟。如果你一直在使用它，你可能是通过 `po` 命令打印类型。在这些情况下，检查该协议的实现；如果它只使用字符串插值和计算属性，你可以改用宏而不是协议。这些类型将与调试器集成得更好，并且你可以专注于一个用于调试的命令：`p`。

我们现在已经多次经历了调试循环，并且探索了有效使用调试器所需的主要概念。我们介绍了如何将调试视为一个搜索问题。通过条件断点和变量检查，LLDB 是有效执行此搜索的强大工具。它也是帮助我们理解不熟悉的代码库的绝佳工具。通过断点动作和表达式计算，我们可以利用我们的编码技能在调试时执行代码。而且我们不需要重新编译项目就能做到！希望你能通过运用我们今天介绍的思想更快地找到错误。一旦你识别出错误的根本原因，记得为那个场景添加测试覆盖，这之前可能缺失。感谢你的观看！
