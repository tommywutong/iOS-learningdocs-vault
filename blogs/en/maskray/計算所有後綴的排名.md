---
title: 計算所有後綴的排名
source: MaskRay (宋方睿)
source_key: maskray
source_url: 'https://maskray.me/blog/2012-01-08-ranking-suffixes'
original_language: en
published: 2012-01-08
status: active
license: 未声明 → 仅私有归档
archived_at: 2026-07-27
content_hash: 'sha256:f6a871bd21c552d1'
translated: false
---

> 原文：[計算所有後綴的排名](https://maskray.me/blog/2012-01-08-ranking-suffixes)　·　MaskRay (宋方睿)

[2012-01-08](https://maskray.me/blog/2012-01-08-ranking-suffixes)

# 計算所有後綴的排名

```e-content
import Control.Applicative
import Control.Arrow
import Control.Monad
import Control.Monad.Instances
import Data.Array
import Data.Function
import Data.List

rank :: (Ord a) => [a] -> [Int]
rank = elems . liftM2 array ((,) 0 . pred . length) (map (first snd) . concat . label . groupBy ((==) `on` fst) . sort . flip zip [0..])
  where
    label = zipWith (flip (map . flip (,))) <*> scanl (+) 0 . map length

rankTails1 :: (Ord a) => [a] -> [Int]
rankTails1 = liftM3 applyUntil (((and . elems).) . (.flip zip (repeat True)) . accumArray (||) False . (,) 0 . pred . length) (const $ map reorder (iterate (*2) 1)) rank
  where
    reorder = (rank.) . (zip <*>) . ((++ repeat (-1)).) . drop
    applyUntil p fs x = head . dropWhile (not . p) $ scanl (flip ($)) x fs

main = getLine >>= print . rankTails1
```
