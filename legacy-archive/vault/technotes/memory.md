# memory：technotes

> 上级说明见 `/agent.md`（目录映射、翻译规范）。本文件只记录本模块进度。

## 进度总览（核对时间：2026-07-27）

**798 份 / 799 页，已译 2 份（约 0.25%）。** 该分类由 PR #10（"补入抓取器漏掉的 950 份归档文档，新增 technotes 分类"）新引入，此前仓库里不存在这个分类，因此也从未被纳入过任何翻译批次或索引标题翻译（PR #9 的索引层翻译范围只有 `documentation` 分类，跑在 PR #10 之前，不会覆盖这里）。

绝大部分文档只有 1 页，属于 Apple Technical Note（tn 前缀）的短技术说明，体量小、单篇独立，适合按需零散翻译，不需要像 Cocoa/Windows Views 那样严格按顺序批次推进。

## 已译清单

- `technotes/tb/-New- cdev Messages`（TB15，'"New" cdev Messages'）
- `technotes/tb/'CDEF' Parameters and Bugs`（TB31，'''CDEF'' Parameters and Bugs'）

## 尚未开始的工作

- 索引层（`_indexes/technotes.md` 及子索引 `_indexes/technotes/*.md`、`_indexes/by-type/technical-note.md`）除上述 2 份已同步外，其余仍是英文标题，未翻译。
- 除上述 2 份外，正文完全未译。
- 按 `agent.md` 铁律，标题（索引 + frontmatter）与正文必须同批次一起译。

## 详细计划

完整的 798 份 × 799 页 checklist（按 topic 分组、状态列可勾选）见 `doc/TRANSLATION_PLAN_TECHNOTES.md`。
