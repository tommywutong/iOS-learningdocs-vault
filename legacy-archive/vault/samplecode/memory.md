# memory：samplecode

> 上级说明见 `/agent.md`（目录映射、翻译规范）。本文件只记录本模块进度。

## 进度总览（核对时间：2026-07-27）

**1905 份示例工程 / 24754 页，未启动翻译（0%）。** 这是全仓体量最大的分类，且绝大部分内容是源代码文件（`.m.md`/`.h.md`/`.swift.md` 等，代码本体不译，只译注释），实际"正文翻译"工作量远小于页数字面显示的规模，但仍需先做一次抽样评估再决定是否/如何立项。

## ⚠️ 已知例外：Calculator（疑似部分已译，未登记）

`samplecode/Calculator/`（9 页）：索引里标题已是"计算器"，文档自身 frontmatter title 仍是英文 `Calculator`。抽查代码文件（`.m.md`/`.h.md`）发现相当比例的中文注释翻译痕迹，怀疑是早期一次未被任何计划文档记录的翻译，**完成度未知，处理前请先人工核实**，不要当作全新文档从头译，也不要当作已完成直接跳过。

## 尚未开始的工作

- 未做任何抽样评估，不清楚 1905 份里有多少是"有意义可译"的说明文档 vs 纯代码工程（可能有大量项目只有 README 级别的单页说明 + 一堆源码页面）。
- 如果要启动翻译，建议先按 `agent.md` 的流程做一次抽样统计（参考 `doc/TRANSLATION_PLAN_DOCUMENTATION_PHASE2.md` 的做法），产出一份 `doc/TRANSLATION_PLAN_SAMPLECODE.md` 再执行，不要没有计划就直接开始批量提交。
- **索引标题层同样还没有被翻译过**（不同于 documentation 分类，PR #9 的范围明确只有 documentation，samplecode 索引目前仍是英文标题），如果先做索引层，务必遵守 `agent.md` 里"标题正文必须同批次一起译"的铁律，不要重蹈 PR #9 的覆辙。
